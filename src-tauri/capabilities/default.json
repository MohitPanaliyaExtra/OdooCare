{
  "identifier": "default",
  "description": "Default capability for OdooCARE — file dialog access only",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "dialog:allow-open",
    "fs:allow-read-file",
    "fs:allow-exists"
  ]
}
